# magic-skill-vfx — Session 2026-08-03

## Status
- Plan approved ("승인 — 바로 실행"), execution started, PAUSED by user request at end of day. 0/9 checkboxes complete.

## Completed
- Boulder magic-skill-vfx registered active (now paused).
- task-0 worktree snapshot: .omo/evidence/task-0-dirty-worktree.txt — 104 porcelain entries (rework's ~97 uncommitted files + others). Never commit those.

## Todo 1 blocker (attempted 3x, each aborted)
- User's live Unity Editor (PID 5100, Hub-launched) holds the project → batchmode -runTests / compile fails "Aborting batchmode due to fatal error".
- Do NOT kill user processes (hard rule: only kill processes we spawn).
- Diagnosis inconclusive: log was piped to stdout and swallowed. On resume: always -logFile <real file>. Distinguish (A) CS compile error in stub/test vs (B) project lock. If (B), user must close the Editor first.

## Confirmed facts (do not re-derive on resume)
- Unity exe: D:\coding\6000.3.12f1\Editor\Unity.exe (NOT C:\Program Files\...).
- GOTCHA: never pass -quit with -runTests (quits before tests run).
- Asmdef: Assets/Script/Combat/Combat.asmdef, internal name ClubGame.Combat; EditModeTests references it. Namespace-less convention, NUnit namespace-less tests.
- EditMode baseline 8/8 green. .omo/evidence is gitignored. Pre-existing dirty set ~104-107 entries.
- Attempt 2 may have left files: check Assets/Script/Combat/SpriteVFXAnimator.cs + Assets/Tests/EditMode/SpriteVFXAnimatorTests.cs before recreating.

## Resume steps
1. User closes Unity Editor (or approves a lock-diagnosis run).
2. Re-dispatch Todo 1 with confirmed Unity path + log-file capture.
3. Wave 2 (Todos 2+3 parallel) → Wave 3 (Todo 4) → Wave 4 (Todo 5) → F1-F4.
4. User commits directly per project convention. FILE_MAP.md updated immediately after each todo passes.